/// WaveyEngine — the audio, DSP, and music-theory engine behind Wavey.
///
/// This is a placeholder so the package builds and `swift test` runs green from
/// the Foundation milestone (DEL-179). Real modules land under the sibling
/// folders: `Audio/`, `DSP/`, `Pitch/`, `Theory/`, `Tuner/`, `Match/`,
/// `Sheet/`, `Ingest/`, `ML/` — see the root `CLAUDE.md` for the module map.
public enum WaveyEngine {
    /// Engine version, surfaced so the app can confirm the package is linked.
    public static let version = "0.0.1"
}
